name: Keep Render Service Warm

on:
  schedule:
    - cron: "*/10 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send ping to Render health check
        run: |
          curl --silent --fail ${{ secrets.RENDER_HEALTHCHECK_URL }} || echo "Ping failed"
